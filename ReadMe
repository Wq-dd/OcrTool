#==============================================================
工具作用：可以标注任意四边形，从而框住目标区域。
使用方法：
	1.点击“打开要保存的文件夹”按钮，选择一个文件夹用来存放生成的txt文件（txt文件包含每个框的4个坐标点信息）。
	2.点击“打开图片所在文件夹”按钮，选择图片所在的文件夹，此时页面应该已经有显示图片了。
	3.可以通过“上一张”，“下一张”进行图片切换，注意！就算没有标注也会生成图片对应的txt文件，但此时文件内容为空，
		这在软件的左边第二个大框中可以看到为空。
	4.“快速跳转”按钮，可以让你快速切换到你想要去的图片，方便接着从上次结束时的图片位置开始继续，所以此时得记住上次
		做到哪张图片了，这在“当前图片序号”下面的框中可以看到。
	5.开始标注，要从左上角逆时针顺序使用鼠标点击标注四个点，每标注一个点都可以在左边的四个小框中看到，这四个小框也是
		按照逆时针顺序来显示的。当四个点标注完后四个小框上的大框就会显示4个坐标点（大框只会显示完整的四个点），当大框中有了
		坐标点后就可以点击“显示框”查看标注效果，“撤销显示”可以使已经显示的框隐藏。
	6.如果发现标注错误，或者标注不准，可以使用“clear”按钮清除标注的坐标。
	7.如果一张图片标注完成，则可以点击“saveTxt”按钮将标注的信息保存为txt文件（只能点击一次saveTxt按钮，否则会保存错误！）。
	8.要查看标注是否保存成功，可以点击“下一张”然后点击“上一张”，左边第二个大框中就会显示对应图片txt中的内容，若发现为空，或者
		内容不正确就需要重新标注。
	注：此工具没有自动保存的功能，需要自己点击保存。
#===============================================================